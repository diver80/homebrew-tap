# diver80 Homebrew Tap

Official Homebrew tap for applications and utilities by diver80.

## Installation

To install **Jira Quick Access**:

```bash
brew install --cask --no-quarantine diver80/tap/jira-quick-access
```

> **Note:** The `--no-quarantine` flag is recommended for unnotarized builds to allow macOS to launch the application directly without Gatekeeper quarantine prompts.

To upgrade:

```bash
brew upgrade --cask jira-quick-access
```

To uninstall:

```bash
brew uninstall --cask jira-quick-access
```
